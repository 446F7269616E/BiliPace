# Hourleaf 隐私政策

生效日期：2026-08-09

Hourleaf 是本地优先的网站专注与使用时间扩展。

## 在设备上处理和保存的数据

Hourleaf 会在浏览器本地保存：

- 你主动添加的网站来源、显示名称、开关、时间段、分组额度与到期模式；
- 每日本地分组解锁状态、等待起点和一次性心流延时到期时间；
- 你主动加入计划的 URL、标题、顺序、完成状态、预定时长、到期模式和限时访问授权；
- 本地日期、网站/目标/时间段标识与聚合使用秒数；
- 结束页布局、你主动填写的激励文字，以及可选解锁密码的 SHA-256 校验值（不保存明文）；
- 你主动启用的站点模块和该模块的本地设置。
- 你主动选择导入的本地模块清单、CSS 和用户脚本。

这些数据用于执行专注规则、恢复计划和生成日 / 周 / 月统计。普通计时不会保存完整 URL、网页标题或页面正文。

## 不收集的数据

Hourleaf 不保存普通浏览历史、搜索词、表单内容、Cookie、密码、账号资料、私信或网页正文。当前版本不包含遥测、广告、第三方分析、账户、云备份或跨设备同步，也不会把产品数据发送给开发者或第三方。

商店核心不包含网站专用模块，也不会从 GitHub 或其他服务器下载模块。导入器会拒绝已知危险格式和能力，但这不是通用恶意代码证明；用户仍须阅读并自行承担导入风险。通过检测的用户脚本只能进入浏览器隔离的 User Scripts 环境。

## 权限

- `storage`：保存上述本地设置、计划和聚合统计；
- `idle`：设备空闲或锁定时停止计时（平台支持时）；
- `scripting`：在你已授权的网站注册 Hourleaf 的内容脚本；
- `userScripts`：在 Chromium 使用浏览器隔离环境运行用户主动导入的脚本；Firefox 仅在用户另行授予可选权限后使用；Safari 商店版不使用此权限；
- 可选网站权限：manifest 中的 HTTP/HTTPS 范围只用于让你选择任意普通网站。Hourleaf 不会默认获得所有网站权限；添加网站时只请求该网站的精确来源。

你可以在浏览器扩展设置中撤销某个网站的权限。撤销后，该网站的计时和专注功能会停止。

## 本地模块与网络请求

商店包只包含导入器和校验/执行边界。模块必须由用户通过文件选择器从本机导入；Hourleaf 不读取远程模块目录、不检查更新，也不执行远程 JavaScript 或 WebAssembly。模块清单中的远程文件引用和 CSS 远程导入会被拒绝。

导入的 CSS 和元素规则只应用在模块声明且用户已授权的精确来源。因为浏览器 DNR 无法保持协议、主机和端口的精确来源边界，本地模块中的非空 DNR 规则会被拒绝。Chromium/Firefox 的用户脚本只通过浏览器 User Scripts API 注册到隔离环境；Safari 商店版拒绝导入用户脚本。

你主动在浏览器中打开 GitHub 或其他网站下载文件时，这些网站可能按各自政策处理网络请求；该下载不是由 Hourleaf 发起，也不是 Hourleaf 遥测。

## 保留与删除

数据会保留在浏览器扩展本地存储中，直到你在 Hourleaf 中清空、删除网站、恢复设置或卸载扩展。删除网站会删除其规则和目标；清空统计会删除本地时间桶。卸载后的清理行为也受浏览器和设备备份机制影响。

扩展存储不是密码保险箱，因此 Hourleaf 从设计上避免保存敏感网页内容。分组解锁密码只用于增加操作摩擦，不构成账户安全保护；能够控制你的设备或浏览器 profile 的人可能访问本地数据或绕过本地逻辑。

## 政策变更与联系

若未来新增数据类别、对外传输、同步、账户或第三方处理，我们会先更新本政策和产品内披露，并在需要时取得明确同意。

一般问题可通过仓库 issue 提交，请勿公开个人数据。安全或隐私漏洞请按 [SECURITY.md](./SECURITY.md) 使用私密报告渠道。由于当前数据只保存在你的设备上，维护者无法直接读取、修改或删除这些数据。
