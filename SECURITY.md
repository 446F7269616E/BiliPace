# Security Policy

## Supported versions

安全修复优先提供给最新公开版本。项目进入稳定发布后，维护者会在 release notes 中明确仍受支持的旧版本；在此之前，仅最新版本被视为受支持。

## Reporting a vulnerability

请不要为未修复漏洞创建公开 issue，也不要附上真实用户数据、浏览器 profile、商店凭据或可直接利用的公开 PoC。

优先使用 GitHub 仓库的 **Security → Report a vulnerability**（Private Vulnerability Reporting）。如果该入口尚未启用，请在不披露技术细节的情况下创建普通 issue，请求维护者提供私密报告渠道。

报告建议包含：

- 受影响版本、浏览器和操作系统；
- 漏洞类型、影响和所需前置条件；
- 最小复现步骤或仅含合成数据的 PoC；
- 可能的缓解建议（可选）。

维护者目标是在 7 天内确认收到报告并给出初步分级。修复时间取决于影响、平台审核和安全发布要求；在补丁已广泛可用前，请与维护者协调披露时间。我们会在征得报告者同意后在安全公告中致谢。

## Scope

范围内包括：扩展权限滥用、跨站/扩展页脚本执行、特权消息伪造、隐私数据外传、统计数据越权访问、供应链/更新完整性和可绕过的关键专注规则。

通常不作为安全漏洞：仅影响显示且无安全/隐私后果的站点模块 DOM 兼容问题、需要已完全控制本地设备/profile 的攻击、社会工程和没有可验证影响的自动扫描结果。它们仍可作为普通 bug 报告。

## Research safety

仅使用自己的测试账号和合成数据；不要访问、修改或保留他人数据，不要破坏服务或绕过商店安全控制。发现密钥或用户数据时立即停止并私密报告，不要继续扩散。
